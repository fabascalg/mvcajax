@{
    ViewData["Title"] = "Lista de Personas";
}

<script>
    $(document).ready(function () {
        $("#boton").click(function () {
            $.getJSON("/home/ListaPersonasJSON",function(datos){
                for (var i=0;i<datos.length; i++){
                    $("#mitabla").append("<tr><td>"+datos[i].nombre+"</td><td>"+datos[i].apellidos+"</td></tr>");
                }
                console.table(datos);
            })

        })
    })
</script>
<p>carga desde repositorio, datos en memoria de la app, dos registros y después el botón los vuelve a buscar al repositorio y sin recargar la p´gina </p>
<table id="mitabla">
    <thead>
    <tr>
        <th>Nombre</th>
        <th>Apellidos</th>
    </tr>
    </thead>
    <tbody>
        @foreach (var item in ViewBag.ListaPersonas)
        {
            <tr>
                <td>@item.Nombre</td>
                <td>@item.Apellidos</td>
            </tr>
            
        }
    </tbody>

</table>
<br>
<input type="button" value="aceptar" id="boton" name="boton">

